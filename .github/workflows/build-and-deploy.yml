name: Build and Deploy

on:
  push:
    tags:
      - '*'
    branches-ignore:
      - '**'

jobs:
  build-and-deploy:
    uses: Shawiizz/devops/.github/workflows/build-and-deploy.yml@main
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
